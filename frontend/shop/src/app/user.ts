export interface User {
  name: string;
  password: string;

  /*save(regname : string , regpassword : string): void {
    const tmp = new User({ this.regname, this.regpassword })
    tmp.save()}
*/
}
